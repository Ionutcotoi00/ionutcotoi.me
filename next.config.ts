import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // For GitHub Pages static export (uncomment if using GitHub Pages)
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
  // trailingSlash: true,
};

export default nextConfig;
